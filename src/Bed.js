/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React from "react";
import { useGLTF, useTexture } from "@react-three/drei";
import { useTextureEditorContext } from "./TextureEditorProvider";

export function Bed(props) {
  const { nodes } = useGLTF("/models/Bed.glb");
  const [image] = useTextureEditorContext();
  const textureProps = useTexture({
    map: image,
  });

  return (
    <group {...props} dispose={null}>
      <mesh geometry={nodes.NewBed.geometry} material={nodes.NewBed.material}>
        <meshStandardMaterial {...textureProps} />
      </mesh>
      <mesh
        geometry={nodes.Matress.geometry}
        material={nodes.Matress.material}
        scale={[1.03, 0.18, 1.99]}
      >
        <meshStandardMaterial {...textureProps} />
      </mesh>
    </group>
  );
}

useGLTF.preload("/models/Bed.glb");
